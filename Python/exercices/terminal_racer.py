import random

random.seed

random.randint(0, 10)

def shiftRight(list):
    i=0
    while list[i]!=1:
        i+=1
    a[i]=0
    a[i+1]=1
    a


def trackGeneration(list):
    if (random.random() > 0.5)
        shiftRight(list)
    else shiftLeft(list)